# VideoParty Server Configuration

# Path to your video files directory (required)
MEDIA_DIR=/absolute/path/to/media

# Server port (default: 8080)
PORT=8080

# Allowed CORS origin - MUST be your exact Netlify deployment URL
# Example: https://myapp.netlify.app
ORIGIN=https://<your-netlify-site>.netlify.app

# Optional shared token for API and WebSocket authentication
# If set, clients must provide this token to access the server
# Note: /media/* and /captions/* must accept ?token= for <video>/<track> requests
# SHARED_TOKEN=supersecrettoken
